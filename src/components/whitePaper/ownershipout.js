const Ownershipint = 'With a guarantor backing the tethered token, with the promise to redeem it for the underlying asset at any future point in time, the token can stay in the system and be used for on-chain and off-chain payments.'

export default Ownershipint
