const libraries = 'We expect that developers will use existing libraries to interact with Ethereum Blockchain (for example, JS web3). We will release wrappers for this library and code samples that will simplify the interaction with the JDB and CryDR smart contracts.'
export default libraries
