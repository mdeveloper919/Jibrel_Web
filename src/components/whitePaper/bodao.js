const bodao = 'Once operations have reached steady state, the Board of Directors can be dissolved and replaced with an autonomous regulatory entity, charged with overseeing the operations of the JDB.'
export default bodao
